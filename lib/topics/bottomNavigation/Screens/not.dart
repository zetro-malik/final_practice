
import 'package:flutter/material.dart';

class NotScreen extends StatelessWidget {
 

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Notification Screen',style: TextStyle(fontSize: 30),),
      
    );
    
  }
}